# Local development / testing dependencies
# Cloudflare Workers Python runtime uses Pyodide — these are for local runs only.
fastapi>=0.115.0
pydantic>=2.7.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
